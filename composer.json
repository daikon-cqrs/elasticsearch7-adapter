{
    "name": "daikon/elasticsearch7-adapter",
    "type": "library",
    "description": "Elasticsearch7 adapter for Daikon-CQRS projects.",
    "keywords": [ "elasticsearch", "adapter", "projection" ],
    "homepage": "https://github.com/daikon-cqrs/elasticsearch7-adapter",
    "license": "MPL-2.0",
    "prefer-stable": true,
    "minimum-stability": "dev",
    "require": {
        "php": "^7.4|^8.0",
        "daikon/dbal": "master@dev",
        "daikon/interop": "master@dev",
        "daikon/metadata": "master@dev",
        "daikon/read-model": "master@dev",
        "elasticsearch/elasticsearch": "^7.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.3",
        "sebastian/phpcpd": "^6.0",
        "squizlabs/php_codesniffer": "^3.3",
        "vimeo/psalm": "^4.0"
    },
    "autoload": {
        "psr-4": { "Daikon\\Elasticsearch7\\" : "src" }
    },
    "autoload-dev": {
        "psr-4": { "Daikon\\Tests\\Elasticsearch7\\": "tests" }
    },
    "archive": {
        "exclude": [
            "/build",
            "/vendor"
        ]
    },
    "scripts": {
        "build": [ "@sniff", "@tests" ],
        "code-sniffer": "phpcs -p -s --extensions=php --standard='PSR1,PSR2' src tests",
        "code-sniffer-autofix": "phpcbf --standard=PSR2 -d tabWidth=4 src tests",
        "copy-paste-detector": "phpcpd src tests || true",
        "psalm": "psalm --shepherd --show-info=true",
        "sniff": [
            "@code-sniffer",
            "@copy-paste-detector",
            "@validate-json",
            "@psalm"
        ],
        "test": "phpunit --no-coverage",
        "tests": "phpunit",
        "validate-json": "composer validate --no-check-all --no-check-lock",
        "validate-deps": "composer validate --with-dependencies --strict"
    }
}
